<%= form_for(current_user.ownerships.find_by(owned_id: @game.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Remove from Library", class: "btn btn-large btn-success" %>
<% end %>