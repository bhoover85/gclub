<% wishlist_form_id = "wishlist_form_#{@game.id}" %>

<div class='<%= wishlist_form_id %> inline-block'>
  <% if current_user.wished?(@game) %>
    <%= render 'games/actions/remove_from_wishlist' %>
  <% else %>
    <%= render 'games/actions/add_to_wishlist' %>
  <% end %>
</div>
