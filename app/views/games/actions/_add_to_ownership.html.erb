<%= form_for(current_user.ownerships.build(owned_id: @game.id), remote: true) do |f| %>
  <div><%= f.hidden_field :owned_id %></div>
  <%= f.submit "Owned", class: "btn btn-large btn-default" %>
<% end %>