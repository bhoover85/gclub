<%= form_for(current_user.wishlists.find_by(wished_id: @game.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Wanted", class: "btn btn-large btn-primary" %>
<% end %>