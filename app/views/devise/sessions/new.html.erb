<% provide(:title, 'Sign in') %>

<div class="row ad-margin">
  <div class="col-sm-6">
    <h2>Sign in</h2>

    <p>
      You can sign in using a variety of methods. If you have a Great Games Club account, use the sign in form below. If you've 
      signed up with an external account (or have yet to sign up), you can use one of the providers listed to the right.
    </p>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email, required: false, autofocus: true %>
        <%= f.input :password, required: false %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Sign in", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <div class="col-sm-6 gray-vertical-border">
    <%= render "devise/shared/links" %>
  </div>
</div>