<% provide(:title, "Great Games of #{@filter}") %>

<div class="row">

  <!-- Left Column -->
  <% if user_signed_in? %>
    <div class="col-md-1">
      <%= render 'shared/user_info' %>
      <%= render 'shared/stats' %>
    </div>
  <% end %>

  <!-- Middle Column -->
  <% if user_signed_in? %>
    <div class="col-md-8">
  <% else %>
    <div class="col-md-9">
  <% end %>
      <h2>
        <i class="fa fa-trophy"></i> <%= "Great Games of #{@filter}" %>
      </h2>
      <h4>
        <i class="fa fa-filter"></i>  
        <%= link_to "All", root_url %> 
          <% Game.select("DISTINCT(YEAR)").reverse.each do |game| %>
            | <%= link_to game.year, "/year/#{game.year}" %>
          <% end %>
      </h4>

      <ul class="games">
        <% @games.each do |game| %>
          <% @game = game %>
          
          <li>
            <section class="pull-left">
              <h3><%= link_to game.name, game %> <small><%= "#{game.platform}" %></small></h3>
              <blockquote>
                <%= image_tag(game.cover.url(:thumb), class: "pull-left game-thumbnail") %>
                <p><%= truncate(game.description, length: 700) %></p>
                <footer>Metacritic</footer>
              </blockquote>
            </section>

            <%= render 'users/wishlist_ownership_admin_buttons' %>

          </li>
        <% end %>
      </ul>

      <div class="text-center">
        <%= will_paginate @games %>
      </div>
    </div>

  <!-- Right Column -->
  <div class="col-md-3">
      <%= render 'layouts/about' %>
      <%= render 'layouts/vertical_ad' %>
  </div>
</div>
