<% ownership_form_id = "ownership_form_#{@game.id}" %>

$(".<%= ownership_form_id %>").html("<%= escape_javascript(render('games/add_to_ownership')) %>")
$("#owned").html('<%= current_user.owned_games.count %>')